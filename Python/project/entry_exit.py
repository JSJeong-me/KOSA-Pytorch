import sys
import time

def entry_exit():
    '''
    for i in range(2):
        sys.stdout.write('{:03d} '.format(i))
        time.sleep(1)
    #   sys.stdout.write('\r')
    '''
    no_entry, no_exit = 10, 28
    return no_entry, no_exit